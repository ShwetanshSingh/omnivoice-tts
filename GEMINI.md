# Project Context

This project uses the `uv` package manager for dependency management and environment handling.

## Development Workflow

- Use `uv run <command>` to execute scripts within the project environment.
- Use `uv add <package>` to add new dependencies.
- Use `uv sync` to synchronize the virtual environment with `pyproject.toml`.
